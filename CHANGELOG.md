# 更新日志

本项目仍在持续迭代。版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## 0.2.0 —— 2026-07-28

自 0.1.0 起,界面外壳、对话区渲染与 SDK 能力接入都做了较大改动;
0.1.0 安装包无法启动子进程的问题也在这一版修复。

### 新增

- **应用图标。** 此前用的是 Electron 默认图标。图形由
  `scripts/make-icon.mjs` 从几何定义直接生成 SVG 与各尺寸 PNG,
  不引图形库,也不会出现 SVG 与位图两份不同步。

### 新增

- 会话右键菜单:重命名、打标签、从这条分支、在资源管理器中打开目录、删除
  (删除是真删 Claude Code 的会话记录,因此有二次确认)
- 斜杠命令面板:命令列表由 SDK 运行时提供,按「内置 / 项目命令 / Skill」分组
- MCP 服务的表单与授权卡(`onElicitation`),按 JSON Schema 通用渲染
- 「Claude 反问你」分步卡与计划卡(见下方已知限制)
- 对话区的四种工具行:Read / Bash / Edit(带 diff)/ TodoWrite
- `.claude` 配置中栏:读写项目自己的 `CLAUDE.md`、`settings.json`、`commands/`、
  `skills/`。路径做了硬性收敛,越界一律拒绝;JSON 存盘前校验并指出出错行号;
  改了没存时切项目会先问,不会默默丢掉
- 子进程面板:后台任务胶囊,可展开查看、单独停止,或把当前任务转入后台
- 从任意一条消息分支重答,可选把文件一并回退到那一刻
  (「能回退几个文件」的数字来自真实的 dry run,回退不可用时禁用并写明原因)
- 控件条三个弹层:权限四档、模型、努力程度五档停靠轨
- 侧栏按「项目 → 会话」两层分组,支持多个工作目录并列
- 额度与上下文占用、本会话花费、版本行
- 深色主题,开关挂在侧栏底部的版本行上(跟随系统 / 始终亮色 / 始终深色)
- 首次配置页:检测 Claude Code CLI、一键安装、凭据加密保存

### 新增(窗口与外壳)

- **无边框窗口 + 自绘标题栏。** 系统标题栏和界面是两张皮 —— 自己的底色、字体、
  高度都对不上。现在窗口按钮由应用自己画,标题栏与侧栏同底,视觉上是一整块。
  加载页、引导页、选择项目页也都带这条标题栏,否则那几屏连关闭按钮都没有。
- **侧栏可收起**,收起后栅格直接去掉那一列。
- **搜索面板**:标题栏放大镜打开,跨全部项目按标题、首句、项目名过滤,
  ↑↓ 选择、Enter 打开、Esc 关闭。只搜标题与首句,不搜正文 ——
  那需要把每个会话的全文读进内存。
- **系统设置对话框**取代原来齿轮上那个小浮窗,包含:
  Claude Code 的接管情况(用的是随包分发那份还是 PATH 上的、版本、可执行文件
  路径)、API 端点与密钥、主题、账号与版本。密钥仍只经 safeStorage 落盘,
  界面读不回明文,所以显示的是「已保存」而不是掩码值。
- 新建会话移到品牌行右端(搜索原来的位置),去掉整行入口与快捷键标注。

### 新增

- **文件树取代了 `.claude` 节点。** 每个项目下都有「文件」入口(此前只有当前
  聚焦的那个项目才有 `.claude`),点开在中栏展开整个项目,`.claude` 只是树里的
  一个普通文件夹。一次只列一层 —— 展开哪层问哪层,不会在 node_modules 上爆炸。
  对话区归属行右端也放了一个入口,边聊边翻文件时手不用跑回侧栏。
- **看得见不等于能改。** 树里能浏览整个项目,但可写的仍然只有 `.claude/` 和
  项目根的 `CLAUDE.md`,其余一律只读并在头部标出来 —— 一个聊天软件不该顺手
  变成可修改任意源码的编辑器。二进制和超过 512 KB 的文件直接拒绝并说明原因。

### 修复(实测反馈第四批)

- **新建会话后立刻拖 Effort 滑块会报「No conversation found with session ID」。**
  `system/init` 一上来就给了 session_id,但那时会话文件还没写;切 Effort 需要
  重开 query 并 `resume`,拿这个 id 去续就找不到。现在只有真发过消息(或本来
  就是从已有会话续上来的)才带 resume。

- **切走再切回来,斜杠命令变成一坨标记。** CLI 把 `/config` 展开成
  `<command-name>…</command-name><command-message>…` 存进会话文件,重建历史时
  照原样画了出来 —— 同一条消息两副面孔。现在还原成 `/config`。
- **删除会话后它还赖在列表里。** `deleteSession` 返回之后紧接着 `listSessions`,
  拿回来的往往仍带着它。现在先在本地划掉,等真实列表也不含它了再放手。
- **「展开全部 N 条」是张单程票**,展开后没有回头路。现在同一颗按钮可以收回去。
- 未保存时点「返回」和点「关闭」,确认之后会按当初点的那一颗走 ——
  从文件树点进来的文件按返回,不该把整栏关掉。

### 新增(把没接的原生能力补上)

- **`/mcp` 面板可以点进去**:展开某个服务能看到它的工具清单(带只读 / 破坏性标注),
  每个服务都有启停,连不上的有重连。此前只是一张只读的列表,而原生的 `/mcp`
  是可以回车进去操作的。
- **`/agents`**:列出本会话可用的子 Agent 及其描述与所用模型。
- **齿轮浮窗里显示账号**:邮箱、组织、订阅类型,非 Anthropic 登录时标出 provider。

这三项此前都不是「SDK 没有」,是我没接 —— `reconnectMcpServer`、`toggleMcpServer`、
`supportedAgents`、`accountInfo` 一直在 `Query` 上。CLAUDE.md 里新增了一张能力
清单,把 27 个控制方法逐个标了「用在哪 / 还没接」,免得再靠一条条试出来。

### 修复(实测反馈第三批)

- **命令面板排序离谱。** 敲 `/mcp`,前三条是 `/fewer-permission-prompts`、
  `/doctor`、`/claude-api` —— 它们只是描述里出现了 MCP 三个字母,而 100% 匹配的
  `/mcp` 排在最后。列表本身来自 SDK,但排序是这边的事,写错了。现在按相关度排:
  名字全等 > 别名全等 > 名字前缀 > 别名前缀 > 名字包含 > 别名包含 > 描述包含;
  分组只作同分时的次序,不再压过相关度。
- **更要命的是同一个面板里有两份顺序**:组件自己过滤一遍按分组画,键盘导航用的
  却是另一份 —— 高亮的那条和眼睛看到的那条会对不上。现在索引和视觉出自同一个数组。
- **`/mcp` 现在是真正的服务面板**,不再是一段纯文本。此前那段文本是 CLI 给 SDK
  宿主的**降级回复**(它自己在末尾写着「详情请去终端看」),我把它当成了功能。
  现在数据来自 `mcpServerStatus()`:按来源分组,每个服务标连接状态、工具数、
  失败原因,连不上的给重连入口。
- **新会话真正做到一发就进侧栏。** 上一版没修好:`chat.open()` 一进工作区就建了
  query,`system/init` 那时就带来了 session_id,`session` 事件在用户还没打字时
  就发过了;真发消息时 id 没变,不再触发,于是只剩本轮结束那次刷新。现在发送时
  先乐观摆一条,等真实列表出现这个 id 再让位,中间不闪。
  对应的 e2e 断言也收紧到 3 秒并附加「此刻这一轮还在跑」—— 之前给了 30 秒宽限,
  「答完才出现」也算通过,等于没测。

### 改进(对话区)

- **正文按 Markdown 渲染。** 此前整块当纯文本画,`**粗体**`、列表、
  ` ```代码块``` `、表格的原始符号直接印在界面上。现在粗体、斜体、行内代码、
  链接、标题、引用、分隔线、表格都成型,代码块单独成块并带语言标签与复制按钮。
  解析器是自己写的(没加依赖,原因见 CLAUDE.md 里的 lockfile 那条),
  只产出数据结构、由 React 生成元素,全程不碰 innerHTML,所以模型吐出来的
  HTML 只会被当字面文本显示。31 条单测钉着边界。
- **显示 Claude 的思考过程。** 默认折叠、露一行摘要;正在想的时候自动展开,
  让人看得见它在动。要 `options.thinking = {type:'adaptive', display:'summarized'}`
  才拿得到,只设 effort 是没有的。
- **多行 Bash 命令整段摊开。** 此前不管几行都压进一行、超出截断 ——
  一条 `cd` 加三个 `grep` 的命令,界面上只看得到第一行,而真正改动机器状态的
  是后面那几行。
- 等待状态行补上输出 token 数与当前努力档:`思考中 · 12 秒 · ↓ 1.9k tokens · 高努力`。

### 改进

- 模型列表补上 SDK 一直提供的描述(「Opus 5 with 1M context · Best for everyday,
  complex tasks」),并删掉右侧那列 1/2/3 —— 那是终端里的键盘序号,在鼠标界面里
  不指代任何东西
- 努力程度从「五个点各自可点」改成可以按住拖的停靠式滑块。拖动时只动预览,
  松手才切档 —— 切一档要重开一次 query,拖过五档不能变成五次重连
- 模型不支持努力程度时(Haiku 的 `ModelInfo` 里没有 `supportedEffortLevels`),
  努力控件直接禁用,不再画五个点让人点了不生效
- 侧栏与 `.claude` 配置栏之间的竖线可以拖拽调宽,各自有上下限,并且始终给
  对话区留出最小宽度;宽度记住,下次打开还是这样
- 在输入框里敲 `/model`、`/effort` 会直接打开对应的浮层。此前点「发送」会把它们
  当普通消息发给 agent,而 agent 不处理界面级命令,消息就石沉大海了
- 归属行右端的「上下文 x%」换成输入框旁的环形入口,和权限、模型、努力放在一起 ——
  它们同属「发这条之前要知道的事」。点开是分类明细:消息、系统工具、MCP 工具、
  Skill、记忆文件各占多少,下面接额度的两条进度条。百分比只说明快满了,
  明细才能说明该删什么;这份数据 SDK 一直在给,此前未被取用
- 侧栏底部的「本会话 $x.xx」换成齿轮:主题、版本、本会话花费都收进浮窗。
  齿轮一眼就是设置,而一串版本号不是
- 新会话在**发出消息的那一刻**就进侧栏,不必等这一轮答完。标题仍由 Claude 生成,
  本轮结束时刷新盖上去
- Claude 思考、跑工具时不再是一片空白:给出还在动的指示点、已用时长、本轮输出的
  token 数。状态文案取自 SDK 自己报的 `requesting` / `compacting`,不自己编;
  token 数不是逐字跳的(usage 只在每条助手消息收尾时出现),所以拿到之前不显示,
  而不是先挂一个 0 假装在数

### 修复(实测反馈第二批)

- **「Claude 反问你」的卡片不出现**,工具在无人作答的情况下跑完,模型收到一句
  「The user did not answer the questions.」。原因是我把它接在 `onUserDialog` 上,
  而这个工具走的其实是 `canUseTool` —— 答案要通过 `updatedInput.answers` 回传,
  键是**题干原文**。计划卡同理(`canUseTool('ExitPlanMode', { plan })`)。
  两者现在都有端到端测试守着。
- **`/mcp`、`/cost` 这类内置命令敲下去没有任何反应。** 正文平时靠流式增量渲染,
  但这些命令的回复由 CLI 一次性合成、整条送达,不带任何增量 —— 只认增量的话
  它们在界面上凭空消失,而且安静得没有报错。任何不走流式的助手消息都受影响。

### 修复

- **安装后打不开对话**:打包时 SDK 会去 `app.asar` 内部启动 claude 可执行文件,
  而 asar 里的可执行文件是起不来的。SDK 拿不到具体原因,报了个 libc 不匹配,
  跟真实原因毫无关系。现在显式指向 `app.asar.unpacked` 下的真身。
  只有 0.1.0 的安装包受影响,源码跑 `npm run dev` 一直是好的。
- 版本行里的 CLI 版本此前报的是 PATH 上那份全局 CLI,而实际启动的是应用自带的
  那份。现在报的是真正会被启动的那一个;没装过全局 CLI 的用户也不会再被引导页
  拦下来,让他去装一个应用其实用不到的东西。

### 已知限制

- 收到无法识别的对话框时一律安全取消并提示,不猜参数硬画一个选择框。
- 文件回退依赖 Claude Code 的文件检查点。会话本身没有检查点时(比如那一轮没动过
  文件),只能分支对话、不能回退磁盘,界面会说明原因而不是悄悄跳过。
- 仅支持 Windows。
- 尚未实现:跨会话搜索。

## 0.1.0

首个可运行版本:通过 `@anthropic-ai/claude-agent-sdk` 驱动本机 Claude Code,
实现聊天、切换模型、会话列表三项基本能力,并打通 tag 触发的自动发布流程。
