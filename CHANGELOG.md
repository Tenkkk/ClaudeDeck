# 更新日志

本项目仍在持续迭代。版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## 未发布

### 新增

- 会话右键菜单:重命名、打标签、从这条分支、在资源管理器中打开目录、删除
  (删除是真删 Claude Code 的会话记录,因此有二次确认)
- 斜杠命令面板:命令列表由 SDK 运行时提供,按「内置 / 项目命令 / Skill」分组
- MCP 服务的表单与授权卡(`onElicitation`),按 JSON Schema 通用渲染
- 「Claude 反问你」分步卡与计划卡(见下方已知限制)
- 对话区的四种工具行:Read / Bash / Edit(带 diff)/ TodoWrite
- `.claude` 配置中栏:读写项目自己的 `CLAUDE.md`、`settings.json`、`commands/`、
  `skills/`。路径做了硬性收敛,越界一律拒绝;JSON 存盘前校验并指出出错行号;
  改了没存时切项目会先问,不会默默丢掉
- 子进程面板:后台任务胶囊,可展开查看、单独停止,或把当前任务转入后台
- 从任意一条消息分支重答,可选把文件一并回退到那一刻
  (「能回退几个文件」的数字来自真实的 dry run,回退不可用时禁用并写明原因)
- 控件条三个弹层:权限四档、模型、努力程度五档停靠轨
- 侧栏按「项目 → 会话」两层分组,支持多个工作目录并列
- 额度与上下文占用、本会话花费、版本行
- 深色主题,开关挂在侧栏底部的版本行上(跟随系统 / 始终亮色 / 始终深色)
- 首次配置页:检测 Claude Code CLI、一键安装、凭据加密保存

### 已知限制

- `AskUserQuestion` 与 `ExitPlanMode` 目前不出现在 Agent SDK 会话的工具列表里,
  所以「Claude 反问你」与「计划卡」两张卡虽然已经实现,但触发不到,
  也因此**未经端到端验证**。收到无法识别的对话框时一律安全取消并提示。
  已试过的配置与实证契约见 [CLAUDE.md](CLAUDE.md)。
- 文件回退依赖 Claude Code 的文件检查点。会话本身没有检查点时(比如那一轮没动过
  文件),只能分支对话、不能回退磁盘,界面会说明原因而不是悄悄跳过。
- 仅支持 Windows。
- 尚未实现:跨会话搜索。

## 0.1.0

首个可运行版本:通过 `@anthropic-ai/claude-agent-sdk` 驱动本机 Claude Code,
实现聊天、切换模型、会话列表三项基本能力,并打通 tag 触发的自动发布流程。
